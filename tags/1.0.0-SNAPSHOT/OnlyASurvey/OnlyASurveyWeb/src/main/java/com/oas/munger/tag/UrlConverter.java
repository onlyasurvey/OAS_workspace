package com.oas.munger.tag;

public class UrlConverter {

	// /**
	// * Convert a parameter name to a munged one.
	// *
	// * @param name
	// * @return
	// */
	// public static String convertParameterName(String name) {
	//
	// String retval = EncoderDecoder.encode(name);
	// return retval;
	// }

	// public static String encodeUrl(String url) {
	//
	// if (!excludeUrlFromEncoding(url)) {
	// return EncoderDecoder.encode(MungerUtil.stripContextPath(url));
	// } else {
	// return url;
	// }
	// }

	// private static boolean excludeUrlFromEncoding(String url) {
	// if ("/".equals(url)) {
	// return true;
	// }
	//
	// if (url.indexOf(":") != -1) {
	// // absolute URL
	// return true;
	// }
	//
	// return false;
	// }
}
